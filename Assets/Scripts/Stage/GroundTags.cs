using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GroundTags : MonoBehaviour {

    public enum GroundTagsEnum {
        Ground,
        Wood,
        Concrete,
    }
    public GroundTagsEnum Tag;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
